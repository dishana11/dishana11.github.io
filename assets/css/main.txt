html, body {
  margin: 0;
  padding: 0;
  min-height: 100%;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  color: #fff;
  overflow: auto;
}

/* JPG background layer (always visible) */
.background-layer {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background: url('{{ site.baseurl }}/assets/my-background.jpg') no-repeat center center fixed;
  background-size: cover;
  z-index: -2;
}

/* GIF layer (page-specific) */
.gif-layer {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  z-index: -1;
  overflow: hidden;
}

.gif-layer img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  opacity: 0.65; /* Adjust transparency so JPG shows through */
}

/* Content wrapper */
.page-content {
  position: relative;
  z-index: 1;
}

/* Navigation bar */
.navbar {
  width: 100%;
  padding: 15px 30px;
  background-color: rgba(26, 26, 26, 0.9);
  display: flex;
  justify-content: center;
  gap: 40px;
  font-size: 16px;
  position: fixed;
  top: 0;
  z-index: 2;
}
.navbar a {
  color: #fff;
  text-decoration: none;
  cursor: pointer;
  padding: 8px 20px;
  border-radius: 7px;
  transition: box-shadow 0.2s;
  font-weight: 500;
}
.navbar a:hover,
.navbar a:focus {
  box-shadow: 0 4px 18px 0 rgba(0, 0, 0, 0.22), 0 1.5px 4px 0 rgba(0,255,195, 0.25);
  outline: none;
}

/* Content box styling */
.content-box {
  max-width: 800px;
  margin: 100px auto 20px auto;
  background-color: rgba(0, 0, 0, 0.8);
  border-radius: 15px;
  padding: 20px;
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.4);
}

/* Intro text style */
.intro-text {
  font-size: 1.2em;
  font-weight: 500;
  background: rgba(255,255,255,0.58);
  border-radius: 10px;
  padding: 15px 20px;
  line-height: 1.45;
  margin-bottom: 20px;
  color: #000;
}

/* Headings */
h1 {
  color: #fff;
  font-size: 2em;
  font-weight: 700;
  margin: 0 0 20px 0;
}

/* Lists */
ul {
  list-style-type: disc;
  padding-left: 20px;
  color: #f4f4f4;
}
li {
  margin-bottom: 15px;
  font-size: 1.05em;
  line-height: 1.6;
}
strong {
  color: #00ffc3;
  font-weight: 600;
}
a {
  color: #00ffc3;
  text-decoration: underline;
}
a:hover {
  color: #00e0a3;
}

/* Focus outline for accessibility */
:focus {
  outline: 2px solid #00ffc3;
}
